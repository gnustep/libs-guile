;
;	Run the 'greg' script on the 'tests' tool to provide output
;	for us to check.
;

(greg-child "/bin/sh" "-c" "greg --outdir tests --tool tests --file greg --verbose")

;(greg-child "greg" "--outdir" "tests" "--tool" "tests" "--file" "greg" "--verbose")

